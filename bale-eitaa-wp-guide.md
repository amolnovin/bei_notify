# 📚 راهنمای ارسال پیام با بله و ایتا در افزونه‌های وردپرس (جایگزین تلگرام)

> استخراج‌شده از مستندات رسمی:
> - **بله:** [docs.bale.ai](https://docs.bale.ai) — «مستندات بازوی بله»
> - **ایتا:** [eitaayar.ir/api](https://eitaayar.ir/api/) (راهنمای رسمی API ایتایار) و [developer.eitaa.com](https://developer.eitaa.com) (درگاه جدید برنامه‌ها)
>
> نسخه افزونه: **3.0.1** — رفع باگ Retry (فقط کانال‌های ناموفق) + سوییچ فعال‌سازی هر پیام‌رسان + زیرمنوی «سایر افزونه‌ها» (فهرست مصرف‌کنندگان REST)؛ شامل صف ناهمزمان، توکن امنیتی، Rate Limit، هدر رسمی تلگرام، Logger و پراکسی/رله

---

## ۱) مقایسه سریع APIها (تلگرام ← بله/ایتا)

| مورد | تلگرام | بله | ایتا |
|---|---|---|---|
| آدرس پایه | `https://api.telegram.org/bot<TOKEN>/` | `https://tapi.bale.ai/bot<TOKEN>/` | `https://eitaayar.ir/api/<TOKEN>/` |
| متد ارسال متن | `sendMessage` | `sendMessage` | `sendMessage` |
| متد تست توکن | `getMe` | `getMe` | `getMe` |
| متد ارسال فایل | `sendDocument` / `sendPhoto` | `sendDocument` / `sendPhoto` / `sendVideo` / `sendAudio` | `sendFile` (تکی برای همه فایل‌ها) |
| دریافت آپدیت | `getUpdates` + وب‌هوک | `getUpdates` + وب‌هوک (پورت 443 و 88) | فقط ارسال (مبتنی بر پنل) |
| ارسال زمان‌بندی‌شده | ❌ | ❌ | ✅ با پارامتر `date` |
| سنجاق پیام | متد جدا `pinChatMessage` | متد جدا `pinChatMessage` | پارامتر `pin=1` در خود sendMessage |
| قالب‌بندی متن | `parse_mode: HTML/Markdown` | Markdown همیشه فعال (بدون parse_mode) | Markdown همیشه فعال (بدون parse_mode) |
| ایتالیک | `_متن_` | `_متن_` (با فاصله دور ستاره/زیرخط) | `__متن__` (دو آندرلاین) |
| فرمت پاسخ | `{ok, result, description}` | `{ok, result, description, error_code}` | `{ok, result}` یا `{ok: false}` |
| محدودیت نرخ | 30 msg/s استاندارد | تعاملی (بر اساس تعامل کاربران) + [API کسب‌وکاری](https://business.bale.ai) پولی برای انبوه | روزانه بر اساس تعامل (برای API «برنامه») |

---

## ۲) دریافت توکن و آماده‌سازی

### 🟢 بله
1. در پیام‌رسان بله با **@botfather** گفتگو کنید ([ble.ir/botfather](https://ble.ir/botfather)) و با دستور `/newbot` ربات بسازید.
2. توکن دریافتی فرمتی مثل `123456789:abcdIuZmK5qNEm2A1BhUaAg7MPJv1O9KCcBQB2ro` دارد.
3. ربات را به کانال/گروه هدف **اضافه و ادمین** کنید.
4. `chat_id` می‌تواند **شناسه عددی** یا **نام کاربری کانال با @** (مثل `@myChannel`) باشد.

> 📌 برای ارسال انبوه با سقف بالاتر، «API کسب‌وکاری» وجود دارد: آدرس `https://tapi.bale.ai/business/bot<TOKEN>/<METHOD>` با شارژ حساب در [business.bale.ai](https://business.bale.ai). متدهای پشتیبانی‌شده: `getMe`, `sendMessage`, `sendPhoto`, `sendVideo`, `sendDocument`, `sendAudio` — فقط برای چت شخصی و ارسال فایل فقط با `file_id`.

### 🟣 ایتا
1. **روش اصلی:** در [پنل ایتایار](https://eitaayar.ir) وارد شوید → در بخش **API** برای هر کانال یک **توکن** بسازید (فرمت: `bot114641:8ac1ab47-...`).
2. **روش جایگزین:** در ایتا با ربات **@botfather** گفتگو و دستور `/newbot` بزنید.
3. ربات را به کانال/گروه **اضافه و ادمین** کنید.
4. `chat_id`:
   - کانال: شناسه عددی از پنل ایتایار **یا** username کانال **بدون @** (مثل `eitaayar`)
   - گروه: لینک دعوت گروه
   - کانال خصوصی: لینک دعوت کانال

### 🟦 تلگرام
1. با ربات **@BotFather** در تلگرام ([t.me/BotFather](https://t.me/BotFather)) گفتگو کنید، با دستور `/newbot` ربات بسازید و توکن بگیرید (فرمت: `123456789:AAF...`).
2. ربات را به کانال/گروه **اضافه و ادمین** کنید.
3. `chat_id`: شناسه عددی یا `@username` کانال — برای چت/گروه خصوصی از «شناسه‌یاب» افزونه استفاده کنید.

---

## ۲.۶) دور زدن فیلترینگ تلگرام در ایران 🛡️

> نکته کلیدی: فیلترینگ فقط **دستگاه‌های داخل ایران** را محدود می‌کند. اگر سایت شما روی سرور **خارج از ایران** است (حالت رایج سایت‌های ایرانی)، ارتباط سرور با `api.telegram.org` آزاد است و **هیچ اقدامی لازم نیست**.

اگر سرور داخل ایران است، ۴ راهکار (هر ۴ در خود افزونه پیش‌بینی شده):

| # | راهکار | کجا | توضیح |
|---|---|---|---|
| ۱ | **آدرس API سفارشی/رله** | تنظیمات ← کارت تلگرام ← فیلد «آدرس API» | یک Reverse Proxy ساده روی سرور شخصی خارج از ایران که `api.telegram.org` را فوروارد می‌کند. مثال Nginx: `proxy_pass https://api.telegram.org;` — سپس آدرس سرور خودتان (مثل `https://relay.my-domain.com`) را وارد کنید. |
| ۲ | **پراکسی داخل افزونه** | تنظیمات ← کارت «پراکسی» | پراکسی HTTP یا SOCKS5 (با پورت و احراز هویت اختیاری). فقط روی درخواست‌های API پیام‌رسان‌های انتخاب‌شده اعمال می‌شود (هوک `http_api_curl` وردپرس) و بقیه ترافیک سایت دست‌نخورده می‌ماند. |
| ۳ | **ثابت‌های سراسری وردپرس** | فایل `wp-config.php` | برای HTTP Proxy: `define('WP_PROXY_HOST','1.2.3.4'); define('WP_PROXY_PORT','1080');` — روی همه درخواست‌های وردپرس اعمال می‌شود. |
| ۴ | **میزبانی خارج از ایران** | — | بهترین و پایدارترین راهکار؛ نه پراکسی لازم است نه رله. |

```bash
# نمونه تنظیم رله (Nginx) روی سرور خارج از ایران:
server {
    listen 443 ssl;
    server_name relay.my-domain.com;
    location / {
        proxy_pass https://api.telegram.org;
        proxy_set_header Host api.telegram.org;
    }
}
```

> 📌 تلگرام از `parse_mode` پشتیبانی می‌کند؛ افزونه به‌صورت خودکار `Markdown` می‌فرستد و اگر متن قالب‌بندی نامعتبری داشته باشد (خطای «can't parse entities»)، ارسال را **بدون قالب‌بندی تکرار می‌کند**.

---

## ۲.۷) استفاده از Nova-Proxy (کلودفلر ورکرز) برای دور زدن فیلترینگ 🛡️

> Nova-Proxy (IRNova) یک پنل ساخت اشتراک VPN روی Cloudflare Workers است که کانفیگ‌های **VLESS / Trojan / WARP** تولید می‌کند — نه یک سرور پراکسی HTTP/SOCKS5. بنابراین:

**❌ مستقیماً به‌عنوان HTTP یا SOCKS5 نمی‌شود** — خروجی Nova-Proxy کانفیگ تونل است که باید با کلاینت (v2rayN، sing-box، Mihomo و...) مصرف شود و هیچ host:port پراکسی‌ای برای فیلد «پراکسی» افزونه نمی‌دهد.

**✅ ولی دو راه عملی دارید:**

### روش A — رله Worker روی همان اکانت کلودفلر (پیشنهادی ⭐)

چون از قبل در کلودفلر اکانت و دامنه دارید، یک Worker کوچک دوم (رله) کنار Nova-Proxy بسازید که فقط درخواست‌های `/bot...` را به `api.telegram.org` فوروارد می‌کند:

1. فایل آماده **`telegram-relay-worker.js`** (در همین پروژه) را در داشبورد کلودفلر ← Workers ← Create Worker جایگزین کنید و Deploy بزنید.
2. در Settings ← Domains & Routes یک Route اضافه کنید: `tg-relay.example.com/*` (دامنه خودتان با Proxy روشن).
3. در افزونه: تنظیمات ← کارت تلگرام ← «آدرس API (سفارشی/رله)» ← مقدار `https://tg-relay.example.com`
4. «تست تلگرام» را بزنید ✅ — یا قبل از اتصال به وردپرس، فایل `test-relay.php` را روی سرور اجرا کنید.

```bash
# تست سریع از خط فرمان سرور:
php test-relay.php
# یا:
curl "https://tg-relay.example.com/bot<TOKEN>/getMe"
```

**امنیت:** اسکریپت رله فقط مسیرهای `/bot...` را قبول می‌کند، پس به پراکسیِ باز تبدیل نمی‌شود.

**نکته کلیدی:** اگر سرور ایران به دامنه کلودفلری شما دسترسی ندارد (کلودفلر در ایران محدود است)، یک **IP تمیز** پیدا کنید (همان اسکنر Nova Radar پنل نوا این کار را می‌کند) و در `/etc/hosts` سرور اضافه کنید:

```
1.2.3.4   tg-relay.example.com
```

سقف رایگان ورکرز ۱۰۰هزار درخواست/روز است — برای اعلان‌ها کاملاً کافی.

### رله چندسایتی (2.10.7) 🌐

فایل `telegram-relay-worker.js` ذاتاً **چندسایتی** است: چون توکن هر ربات داخل مسیر است (`/bot<TOKEN>/sendMessage`)، هر تعداد سایت با ربات‌های مختلف می‌توانند همزمان از یک رله استفاده کنند — کافی است در کارت تلگرام هر سایت فقط آدرس رله را وارد کنید.

برای کنترل اینکه «چه کسی» از رله استفاده کند، دو دربان اختیاری در بالای فایل Worker هست:

| دربان | توضیح |
|---|---|
| `ALLOWED_BOTS` | لیست سفید پیشوند توکن ربات‌ها (مثلاً `['123456789:']`) — فقط این ربات‌ها پذیرفته می‌شوند؛ خالی = همه ربات‌ها |
| `RELAY_KEY` | کلید مشترک — اگر پر باشد، درخواست‌ها باید `?key=...` داشته باشند؛ همین کلید را در «کلید امنیتی رله» کارت تلگرام هر سایت وارد کنید و افزونه خودکار به همه درخواست‌های تلگرام (ارسال، getMe، تصویر، وبهوک، getUpdates) اضافه‌اش می‌کند |

> 🎯 سناریوی پیشنهادی چندسایتی: `RELAY_KEY` را یک مقدار بدهید و `ALLOWED_BOTS` را با پیشوند توکن ربات‌های خودتان پر کنید — نه سایت دیگری می‌تواند استفاده کند، نه ربات دیگری.

### روش B — کلاینت تونل روی خود سرور وردپرس (پیچیده‌تر)

اگر اصرار به استفاده از خود اشتراک Nova-Proxy دارید: یک کلاینت تونل (مثلاً sing-box یا Mihomo) روی **سرور وردپرس** نصب کنید، کانفیگ اشتراک نوا را Import کنید و یک inbound مختلط (HTTP+SOCKS) روی localhost باز کنید. سپس در کارت «پراکسی» افزونه:

- نوع: SOCKS5 — آدرس: `127.0.0.1` — پورت: پورت localhost کلاینت (مثلاً 1080)

این روش کار می‌کند ولی سنگین‌تر است، کل ترافیکِ همان پورت را از تونل رد می‌کند و باید مطمئن شوید اتصال VLESS از شبکه سرور ایران به ورکر نوا برقرار می‌شود (دوباره بحث IP تمیز). روش A ساده‌تر و پایدارتر است.

---

## ۲.۸) استفاده از Nova-Proxy برای وردپرس — راه‌های واقعی 🛡️

> ⚠️ تصحیح مهم: فیلدهای «تنظیمات پراکسی» صفحه Network نوا **ورودی** هستند (نوا از شما یک پراکسی می‌خواهد تا ترافیک خودش را از آن رد کند — مخصوص Backend mode با VPS شخصی). آن‌ها به شما آدرس پراکسی **نمی‌دهند**. توضیح کامل هر فیلد در بخش ۲.۹.

برای هدف ما («افزونه وردپرس → api.telegram.org») این مسیرها عملی‌اند:

### مسیر ۱ — رله Worker کنار نوا (پیشنهادی ⭐، بدون پراکسی)

یک Worker دوم کوچک بسازید که فقط `/bot...` را به `api.telegram.org` فوروارد می‌کند:

1. فایل **`telegram-relay-worker.js`** را در داشبورد کلودفلر ← Workers ← Create Worker جایگزین و Deploy کنید.
2. به دامنه‌تان وصلش کنید (مثل `tg-relay.example.com`).
3. در افزونه: کارت تلگرام ← «آدرس API سفارشی/رله» ← `https://tg-relay.example.com`
4. «تست تلگرام» ✅ — فایل `test-relay.php` هم برای تست مستقل هست.

### مسیر ۲ — پراکسی عمومی از لیست «Proxy / CDN» خود نوا

بخش Proxy/CDN پنل نوا لیستی از پراکسی‌های عمومی (دسته‌بندی بر اساس کشور) دارد. یکی را انتخاب کنید و در **کارت «پراکسی» افزونه** وارد کنید:

- اگر SOCKS5 گرفتید: نوع SOCKS5 + آدرس/پورت + کاربر/رمز همان لیست
- اگر HTTP گرفتید: نوع HTTP + همان مقادیر
- بعد: دکمه «🌐 تست پراکسی» → در صورت سبزشدن «تست تلگرام»

⚠️ طبق FAQ رسمی نوا این پراکسی‌ها **عمومی و تغییرپذیرند** (ممکن است فردا از دسترس خارج شوند) و برای کارهای حساس توصیه نمی‌شوند — ولی برای اعلان‌های ساده سایت قابل قبول‌اند.

### مسیر ۳ — اگر VPS خارج از ایران دارید

پر کردن فیلدهای SOCKS5/HTTP نوا لازم نیست؛ ساده‌تر است رله را روی همان VPS بگذارید (یا اصلاً سایت را آنجا هاست کنید) — آن وقت نه پراکسی لازم است نه رله.

### مسیر ۴ — کلاینت تونل روی سرور وردپرس (سنگین، آخرین گزینه)

اشتراک VLESS نوا را با sing-box/Mihomo روی سرور وردپرس import کنید و یک inbound محلی باز کنید؛ بعد در کارت پراکسی افزونه: SOCKS5 با `127.0.0.1` و پورت محلی. کار می‌کند ولی نگهداری‌اش سنگین است و به IP تمیز برای رسیدن سرور به ورکر نوا نیاز دارد.

---

## ۲.۹) درک فیلدهای «تنظیمات پراکسی» صفحه Network نوا (سردرگمی رایج!)

صفحه `/Nova-Proxy/network` فیلدهای زیر را دارد و **هیچ‌کدام به شما «آدرس پراکسی» نمی‌دهند — همه ورودی هستند، نه خروجی**:

| فیلد پنل نوا | یعنی چه؟ | چه وارد کنم؟ |
|---|---|---|
| 🚀 اشتراک برگزیده | لینک اشتراک پیش‌فرض پنل | ربطی به هدف ما ندارد |
| 🌐 پراکسی‌آی‌پی | IP واسطی که کلاینت برای اتصال به ورکر استفاده می‌کند | فقط برای بهتر کار کردن VPN خودتان در ایران |
| 🔒 SOCKS5 (نمونه: `user:pass@127.0.0.1:1080`) | **پراکسی که نوا باید ترافیکش را از آن رد کند** (زنجیره پراکسی / Backend mode) | اگر VPS خودتان ندارید: **خالی بگذارید** |
| 🌍 HTTP (نمونه: `http://user:pass@127.0.0.1:8080`) | همان بالا، نوع HTTP | اگر VPS خودتان ندارید: **خالی بگذارید** |

**چرا نمونه 127.0.0.1 است؟** چون این فیلدها برای «Backend mode» طراحی شده‌اند: وقتی نوا را به سرور شخصی خودتان (VPS خارج از ایران با Xray/sing-box) وصل می‌کنید، آن سرور یک پراکسی محلی روی `127.0.0.1` باز می‌کند و شما آدرسش را اینجا می‌دهید تا نوا از آن رد شود. یعنی:

- `127.0.0.1` = «همان ماشینی که بک‌اند نوا رویش اجرا می‌شود» — نه کامپیوتر شما.
- اگر بک‌اند/سرور شخصی ندارید، این فیلدها **بی‌استفاده‌اند و باید خالی بمانند** (طبق FAQ رسمی، برای کاربر عادی اجباری نیستند و بیشتر برای سرویس‌های پشت کلودفلر مثل ChatGPT/Twitter کاربرد دارند).

**🎯 پس برای «افزونه وردپرس → تلگرام» چی؟** دو راه واقعی:

1. **رله Worker (پیشنهادی — بدون هیچ پراکسی):** فایل `telegram-relay-worker.js` را یک Worker دوم کنار نوا بسازید و آدرسش را در افزونه ← کارت تلگرام ← «آدرس API سفارشی/رله» بگذارید. تمام.
2. **پراکسی عمومی از لیست خود نوا:** بخش «Proxy / CDN» پنل نوا لیستی از پراکسی‌های عمومی (بر اساس کشور) دارد — می‌توانید یکی را کپی کنید و در کارت «پراکسی» افزونه وردپرس بگذارید + دکمه «تست پراکسی». (هشدار: عمومی و ناپایدارند؛ برای اعلان ساده قابل قبول است)

> اگر یک VPS خارج از ایران دارید و می‌خواهید از همان استفاده کنید: به‌جای راه‌اندازی پراکسی و پر کردن این فیلدها، ساده‌تر است همان رله (یا کل سایت) را روی آن VPS بگذارید.

---

## ۲.۱۰) واتساپ — مسیرهای رایگان (بدون Green API و روش‌های پولی) 💬

دو مسیر واقعاً رایگان و بدون نیاز به سرور شخصی برای ارسال پیام به واتساپ وجود دارد:

| مسیر | هزینه | سرور | قالب | محدودیت |
|---|---|---|---|---|
| **CallMeBot** ⭐ | **رایگان** | ❌ | ❌ | فقط به شماره خودتان (استفاده شخصی) |
| **شماره تست متا (Cloud API)** | **رایگان** | ❌ | ❌ | تا ۵ شماره تأییدشده |
| Green API / Ultramsg | پولی ❌ | ❌ | ❌ | — |
| شماره اصلی متا (Utility/Marketing) | پیام‌به‌پیام پولی ❌ | ✅ وبهوک | ✅ قالب | شماره +98 هم پشتیبانی نمی‌شود |

### مسیر ۱ — CallMeBot (ساده‌ترین، کاملاً رایگان) ⭐

یک درگاه رایگان که کلاینت واتساپ را خودش نگه می‌دارد؛ مخصوص «ارسال پیام به خودتان» که دقیقاً همان کاربرد اعلان‌های سایت است.

> ⚠️ مهم: از خود سایت apikey نمی‌گیرید — هیچ فرمی وجود ندارد. بخش «Setup» صفحه فقط دستورالعمل است؛ apikey را ربات از داخل واتساپ می‌فرستد.

1. شماره ربات CallMeBot را در واتساپ ذخیره کنید (شماره جاری طبق صفحه: **+34 694 29 84 96** — همیشه موقع اقدام از [وبلاگ رسمی](https://www.callmebot.com/blog/free-api-whatsapp-messages/) چک کنید چون عوض می‌شود).
2. دقیقاً این پیام را به آن شماره بفرستید: `I allow callmebot to send me messages`
3. ربات در واتساپ جواب می‌دهد: `API Activated for your phone number. Your APIKEY is 123123` — همان عدد آخر، **apikey** شماست. (اگر تا ۲ دقیقه جواب نیامد، ۲۴ ساعت صبر کنید و دوباره بفرستید.)
4. در افزونه: کارت واتساپ ← درگاه **CallMeBot** ← apikey در «توکن درگاه» + شماره خودتان (با کد کشور، مثل `+4915123456789`) در «شماره مقصد» ← «💬 تست واتساپ» ✅

```bash
# نمونه درخواست (مستندات رسمی CallMeBot):
curl "https://api.callmebot.com/whatsapp.php?phone=+4915123456789&text=سلام&apikey=1234567"
```

> تست از مرورگر (قبل از وردپرس): همان صفحه لینک «Web Browser assistant» دارد — callmebot.com/blog/test-whatsapp-api/

#### ⚠️ خطای «cURL error 28: Connection timed out» هنگام تست واتساپ

این خطا یعنی **سرور سایت نتوانسته به api.callmebot.com وصل شود** (مهلت اتصال ۱۰ ثانیه‌ای وردپرس تمام شده) — ربطی به apikey ندارد. به ترتیب این‌ها را امتحان کنید:

1. **پراکسی خراب؟** اگر در کارت پراکسی سوییچ «واتساپ (CallMeBot)» روشن است ولی پراکسی سالم نیست → خاموشش کنید.
2. **رله (راه حل قطعی ⭐):** فایل `callmebot-relay-worker.js` را دقیقاً مثل رله تلگرام، یک Worker در کلودفلر بسازید (فقط `/whatsapp.php` را فوروارد می‌کند) و آدرسش را در کارت واتساپ ← «آدرس API (سفارشی/رله)» بگذارید. چون رله تلگرام شما از همین سرور کار می‌کند، این هم کار خواهد کرد.
3. **فورس IPv4:** در کارت پراکسی ← بخش «عیب‌یابی شبکه» سوییچ «فورس IPv4» را روشن کنید (و تیک «واتساپ (CallMeBot)» را در «دامنه‌های هدف» بزنید تا روی این دامنه اعمال شود).
4. **مهلت اتصال:** همان بخش، عدد ۳۰ را وارد کنید.
5. **تست خط فرمان روی سرور** (خروجی -v نشان می‌دهد مشکل DNS است یا TCP):
```bash
curl -v "https://api.callmebot.com/whatsapp.php?phone=989393...&text=تست&apikey=6311573"
```

> تست موفق از بیرون: اگر از مرورگر خودتان (یا هر سرور خارجی) لینک زیر را باز کنید و پیام برسد، یعنی apikey سالم است و مشکل فقط شبکه سرور سایت است:
> `https://api.callmebot.com/whatsapp.php?phone=989392021365&text=تست&apikey=6311573`

### مسیر ۲ — شماره تست رایگان متا (رسمی، بدون قالب)

متا در Cloud API یک «شماره تست» رایگان می‌دهد که ارسال پیام به آن **بدون قالب و بدون هزینه** است:

1. در business.facebook.com یک اکانت بیزینس رایگان بسازید.
2. WhatsApp → API Setup ← یک **شماره تست (Test Number)** رایگان + Phone Number ID + توکن می‌گیرید.
3. در بخش **To** شماره گیرنده‌ها (تا ۵ شماره) را Verify کنید.
4. در افزونه: کارت واتساپ ← درگاه **Meta** ← Phone Number ID در «شناسه نمونه» + توکن + شماره مقصد بین‌المللی ← تست ✅

نکته: `graph.facebook.com` در ایران فیلتر است — از «آدرس API سفارشی» (رله Worker مثل رله تلگرام) یا کارت پراکسی استفاده کنید. شماره +98 هم پشتیبانی نمی‌شود.

### نکات مهم

- هر دو مسیر بالا **رایگان و بدون سرور** هستند؛ تفاوت: CallMeBot فقط به خودتان پیام می‌دهد، شماره تست متا تا ۵ شماره.
- گزینه‌های Green API و Ultramsg در افزونه باقی مانده‌اند اما پولی‌اند — فقط برای ارسال انبوه به کاربران (که آن هم برای شماره‌های ایرانی +98 جواب نمی‌دهد).
- واتساپ شماره +98 را نمی‌پذیرد؛ شماره غیرایرانی لازم است (همان شماره‌ای که خودتان برای واتساپ دارید).

---

## ۲.۱۲) پیدا کردن شناسه عددی گفتگو 🔎

### با خود افزونه (ساده‌ترین راه)

در **تنظیمات ← بله و ایتا ← کارت «پیدا کردن شناسه عددی گفتگو»** سه ابزار هست:

| ابزار | بله | تلگرام | ایتا | نحوه کار |
|---|---|---|---|---|
| 📤 خواندن شناسه با ارسال تست | ✅ | ✅ | ✅ | یک پیام تست به chat_id فعلی می‌فرستد و شناسه عددی را از `result.chat.id` پاسخ API استخراج می‌کند |
| 📥 دریافت گفتگوهای اخیر (getUpdates) | ✅ | ✅ | ❌ | آپدیت‌های اخیر ربات را می‌خواند و همه گفتگوها را با شناسه عددی فهرست می‌کند |
| 🤖 ربات پاسخگوی «شناسه شما» | ✅ | ✅ | ❌ | وبهوک ربات را به سایت شما وصل می‌کند؛ هرکس به ربات پیام بدهد، شناسه عددی گفتگو را همراه دکمه «شناسه شما» می‌گیرد |

بعد از پیدا شدن، با دکمه **«استفاده از این شناسه»** مقدار مستقیماً در فیلد chat_id قرار می‌گیرد.

#### عیب‌یابی «ربات پاسخگو جواب نمی‌دهد» (Start چیزی نمایش نمی‌دهد) — 2.10.4

دو جهت ارتباط ربات پاسخگو از هم جداست:

| جهت | مسیر | نیاز به رله؟ |
|---|---|---|
| خروجی: ثبت وبهوک (setWebhook) + **پاسخ‌های ربات** | از «آدرس API (سفارشی/رله)» رد می‌شود (`telegram_base()`) | ✅ بله — به‌صورت خودکار اعمال می‌شود |
| ورودی: دریافت Start از تلگرام | مستقیم از سرور تلگرام به `wp-json/bei/v1/bale-webhook` سایت شما | ❌ رله نقشی ندارد — سایت باید HTTPS معتبر و از بیرون در دسترس باشد |

**قدم‌های تشخیص (به ترتیب):**

1. دکمه جدید **«📡 بررسی وضعیت وبهوک»** را در کارت شناسه‌یاب بزنید — `getWebhookInfo` رسمی تلگرام را نشان می‌دهد:
   - اگر `url` خالی است → وبهوک ثبت نشده؛ «فعال‌سازی ربات پاسخگو» را بزنید.
   - اگر `last_error_message` پر است (مثل «SSL error» یا «Connection timed out») → علت دقیق همین‌جاست: معمولاً مشکل SSL سایت یا در دسترس نبودن سایت از بیرون.
   - اگر آدرس ثبت‌شده پارامتر `for=telegram` ندارد → با نسخه قدیمی ثبت شده؛ یک بار «غیرفعال‌سازی» و دوباره «فعال‌سازی» کنید (در این حالت پاسخ ربات به‌اشتباه از ربات بله ارسال می‌شد!).
2. مطمئن شوید **«آدرس API (سفارشی/رله)» تلگرام تنظیم است** — ثبت وبهوک و پاسخ ربات از همین مسیر رد می‌شوند؛ اگر خالی باشد، پیام موفقیت فعال‌سازی به شما هشدار می‌دهد.
3. اگر سایت روی سرور ایران است و HTTPS ندارد یا CDN/فایروال درخواست‌های خارجی را می‌بندد، تلگرام نمی‌تواند سایت را صدا بزند — `last_error_message` دقیقاً همین را گزارش می‌کند.
4. دکمه **«🧪 تست شبیه‌سازی Start»** (جدید در 2.10.5) دقیقاً مشخص می‌کند زنجیره کجا می‌شکند — دو بخش دارد:
   - **منطق هندلر:** یک آپدیت جعلی `/start` مثل تلگرام ساخته و هندلر واقعی اجرا می‌شود؛ نتیجه نشان می‌دهد پاسخ از مسیر «تلگرام» یا «بله» ساخته شد و متن/دکمه «شناسه شما» چه بود.
   - **مسیر HTTP:** همان درخواست به آدرس وبهوک سایت POST می‌شود (کاری که تلگرام می‌کند) — پاسخ ۲۰۰ یعنی مسیر REST و کلید محرمانه سالم‌اند؛ ۴۰۱/۴۰۳ یعنی کلید یا افزونه امنیتی؛ ۴۰۴ یعنی wp-json بسته شده (Disable REST/پیوندهای یکتا).

**علت رایج «بله کار می‌کند ولی تلگرام نه» (2.10.6):** اگر وبهوک با نسخه قدیمی ثبت شده باشد (آدرس بدون پارامتر `for=`)، همه آپدیت‌ها به مسیر پیش‌فرض «بله» هدایت می‌شوند — Start تلگرام از طریق ربات بله پاسخ می‌گیرد و در تلگرام چیزی دیده نمی‌شود، در حالی که بله سالم کار می‌کند. راه‌حل یک‌کلیکی: دکمه **«🔧 ترمیم خودکار وبهوک»** (setWebhook را با آدرس صحیح `for=telegram` یا `for=bale` دوباره ثبت می‌کند و با getWebhookInfo تأیید می‌کند).

**محتوای وبهوک (آنچه تلگرام بعد از Start می‌فرستد):** یک JSON `Update` شامل `message.chat.id` ، `message.from` و `message.text = "/start"`. ربات پاسخگو بعد از Start باید نمایش دهد:
- Start ساده → «🆔 شناسه عددی این گفتگو: X» + دکمه «شناسه شما»
- Start با پی‌لود فعال‌سازی اعلان (`bei_u_...`) → «✅ شناسه شما دریافت و در حساب کاربری ذخیره شد»
- Start با پی‌لود سفارش (`bei_sub_...`) → «✅ اعلان‌های سفارش #X برای شما فعال شد»

> ❗ چرا ربات پاسخگو در ایتا نیست؟ API ایتایار فقط سه متد دارد (`getMe`، `sendMessage`، `sendFile`) و متد دریافت آپدیت (getUpdates/وبهوک) ندارد؛ بنابراین ربات پاسخگو در بله و تلگرام قابل پیاده‌سازی است. در ایتا شناسه عددی از پنل ایتایار یا با «ارسال تست» به‌دست می‌آید — و اصلاً نیازی به شناسه عددی نیست چون username بدون @ هم به‌عنوان chat_id کار می‌کند.

### روش دستی (بدون افزونه)

**بله:**
```bash
# روش ۱ — بعد از اینکه کسی به ربات پیام داد (چت خصوصی/گروه):
curl "https://tapi.bale.ai/bot<TOKEN>/getUpdates"
# → نتیجه: ... "chat":{"id": 123456789, ...}

# روش ۲ — ارسال به کانال با @username و خواندن شناسه از پاسخ:
curl -X POST "https://tapi.bale.ai/bot<TOKEN>/sendMessage" \
  -H "Content-Type: application/json" \
  -d '{"chat_id": "@myChannel", "text": "تست"}'
# → پاسخ: "result":{"chat":{"id": 123456789, ...}}
```

**ایتا:**
```bash
# ارسال به username کانال یا لینک دعوت گروه و خواندن شناسه از پاسخ:
curl -X POST "https://eitaayar.ir/api/<TOKEN>/sendMessage" \
  -d 'chat_id=myChannel&text=تست'
# → پاسخ: "result":{"chat":{"id": 112233, ...}}
```
- شناسه عددی هر کانال در پنل ایتایار (بخش کانال‌ها) هم نوشته شده است.

---

## ۳) API بله — جزئیات مستندات رسمی

### قوانین کلی درخواست
- پروتکل HTTPS؛ متدهای `GET` و `POST` هر دو پشتیبانی می‌شوند.
- ۴ روش ارسال پارامتر: query string، `application/x-www-form-urlencoded`، `application/json` (به‌جز آپلود فایل)، `multipart/form-data` (برای آپلود).
- پاسخ همیشه JSON با فیلد `ok` است؛ در خطا `error_code` و `description` دارد.
- نام متدها به حروف بزرگ/کوچک حساس نیست.

### sendMessage
| پارامتر | نوع | الزام | توضیح |
|---|---|---|---|
| `chat_id` | String/Integer | ✅ | شناسه گفتگو یا `@username` کانال |
| `text` | String | ✅ | متن پیام، ۱ تا ۴۰۹۶ کاراکتر |
| `reply_to_message_id` | Integer | ❌ | پاسخ به یک پیام |
| `reply_markup` | JSON | ❌ | دکمه‌های Inline/Reply |

### قالب‌بندی Markdown (بله)
- **پررنگ:** `**متن**` — با یک فاصله قبل از ستاره اول و بعد از ستاره دوم
- *کج:* `_متن_` — با فاصله اطراف زیرخط‌ها
- **لینک:** `[متن](آدرس)` — بدون فاصله بین کروشه و پرانتز

### ارسال فایل
- با `file_id` (نامحدود)، با URL (تصویر حداکثر ۵MB و بقیه ۲۰MB)، یا آپلود multipart (تصویر ۱۰MB، بقیه ۵۰MB).
- در `sendDocument` با URL فقط GIF/PDF/ZIP کار می‌کند.

### نمونه curl (از مستندات رسمی)

```bash
# تست توکن
curl "https://tapi.bale.ai/bot<TOKEN>/getMe"

# ارسال پیام
curl -X POST "https://tapi.bale.ai/bot<TOKEN>/sendMessage" \
  -H "Content-Type: application/json" \
  -d '{"chat_id": "@myChannel", "text": "سلام از بله"}'

# ارسال تصویر با URL
curl -X POST "https://tapi.bale.ai/bot<TOKEN>/sendPhoto" \
  -H "Content-Type: application/json" \
  -d '{"chat_id": "@myChannel", "photo": "https://example.com/img.jpg", "caption": "عکس"}'

# ارسال تصویر با آپلود فایل
curl -X POST "https://tapi.bale.ai/bot<TOKEN>/sendPhoto" \
  -F "chat_id=@myChannel" \
  -F "photo=@/path/to/photo.jpg" \
  -F "caption=عکس"

# API کسب‌وکاری (سقف بالاتر)
curl -X POST "https://tapi.bale.ai/business/bot<TOKEN>/sendMessage" \
  -H "Content-Type: application/json" \
  -d '{"chat_id": 123456789, "text": "سلام"}'
```

---

## ۴) API ایتا — جزئیات مستندات رسمی (ایتایار)

### قوانین کلی
- آدرس: `https://eitaayar.ir/api/TOKEN/METHOD_NAME`
- متدهای موجود: `getMe` ، `sendMessage` ، `sendFile`
- حساس به بزرگی حروف نیست؛ `GET` و `POST` و چهار روش ارسال پارامتر (query / json / form-urlencoded / multipart) پشتیبانی می‌شوند.
- پاسخ: `{"ok": true, "result": {...}}` در موفقیت و `{"ok": false}` در خطا.
- صفحه تست رسمی: [eitaayar.ir/testApi](https://eitaayar.ir/testApi)

### sendMessage
| پارامتر | الزام | توضیح |
|---|---|---|
| `chat_id` | ✅ | شناسه کانال/گروه (عددی از پنل یا username بدون @) |
| `text` | ✅ | متن پیام |
| `title` | ❌ | عنوان مطلب (فقط در پنل ایتایار) |
| `date` | ❌ | زمان ارسال زمان‌بندی‌شده به صورت **Unix Timestamp** |
| `pin` | ❌ | سنجاق پیام (مقدار `1`) |
| `viewCountForDelete` | ❌ | حذف خودکار پس از N بازدید |
| `disable_notification` | ❌ | ارسال بدون اعلان |
| `reply_to_message_id` | ❌ | پاسخ به یک پیام |

### sendFile
- پارامتر `file` (الزامی، multipart) به‌جای text از `caption` استفاده کنید.
- ⚠️ گیف باید `.gif` باشد و استیکر باید `.webp` باشد (در غیر این صورت تبدیل نمی‌شود).

### قالب‌بندی Markdown (ایتا)
| قالب | نشانه |
|---|---|
| پررنگ | `**متن**` |
| کج | `__متن__` |
| کد درون‌خطی | `` `کد` `` |
| بلوک کد | ```` ```کد``` ```` |
| لینک | `[متن](آدرس)` |
| نمایش خود نشانه | `\*` |

### نمونه curl (از مستندات رسمی)

```bash
# تست توکن
curl "https://eitaayar.ir/api/<TOKEN>/getMe"

# ارسال پیام (JSON)
curl -X POST "https://eitaayar.ir/api/<TOKEN>/sendMessage" \
  -H "Content-Type: application/json" \
  -d '{"chat_id": "myChannel", "text": "سلام از ایتا"}'

# ارسال زمان‌بندی‌شده + سنجاق + حذف بعد از 100 بازدید
curl -X POST "https://eitaayar.ir/api/<TOKEN>/sendMessage" \
  -H "Content-Type: application/json" \
  -d '{"chat_id": "myChannel", "text": "پیام زمان‌بندی‌شده", "date": 1791000000, "pin": 1, "viewCountForDelete": 100}'

# ارسال فایل
curl -X POST "https://eitaayar.ir/api/<TOKEN>/sendFile" \
  -F "chat_id=myChannel" \
  -F "caption=توضیح فایل" \
  -F "file=@/path/to/file.pdf"
```

### API جدید «برنامه» ایتا (برای ارسال به کاربران)
- آدرس: `https://eitaayar.ir/api/app/sendMessage`
- بدنه: `{"token": "...", "chat_id": 279058397, "text": "سلام"}`
- ✅ موفقیت: `{"ok": true, "result": "success"}`
- ⚠️ کاربر باید قبلاً برنامه شما را Start کرده باشد؛ هر برنامه روزانه «اعتبار ارسال» می‌گیرد و هر پیام ۱ اعتبار کم می‌کند.
- مستندات: [developer.eitaa.com](https://developer.eitaa.com/docs/Develop/SendMassage/)

---

## ۵) افزونه وردپرس (آماده استفاده)

فایل `wp-bale-eitaa-notifier/wp-bale-eitaa-notifier.php` یک افزونه کامل است:

### ساختار استاندارد (WordPress Plugin Handbook / WPCS)

```
wp-bale-eitaa-notifier/
├── wp-bale-eitaa-notifier.php          ← فایل اصلی (بوت‌استرپ + توابع میان‌بُر)
├── readme.txt                          ← معرفی استاندارد افزونه
├── uninstall.php                       ← پاک‌سازی کامل داده‌ها هنگام حذف
├── includes/
│   ├── class-bei-plugin.php            ← کلاس اصلی (Singleton) — بارگذاری ماژول‌ها
│   ├── class-bei-settings.php          ← تنظیمات: Settings API، صفحه مدرن، تست اتصال
│   ├── class-bei-messenger.php         ← ارسال به API بله و ایتا
│   ├── class-bei-email-bridge.php      ← پل ایمیل (فیلتر wp_mail)
│   ├── class-bei-integrations.php      ← اتصال CF7/WPForms/ووکامرس/...
│   ├── class-bei-rest.php              ← REST API (wp-json/bei/v1/notify)
│   ├── class-bei-id-finder.php         ← شناسه‌یاب + ربات پاسخگوی «شناسه شما»
│   └── class-bei-proxy.php             ← پراکسی (دور زدن فیلترینگ) روی http_api_curl
└── assets/
    ├── css/admin.css                   ← استایل مدرن صفحه تنظیمات
    └── js/admin.js                     ← دکمه کپی (بدون وابستگی به jQuery)
```

استانداردهای پیاده‌سازی‌شده:

| استاندارد | وضعیت |
|---|---|
| معماری شیءگرا (OOP) با کلاس‌های مجزا + الگوی Singleton | ✅ |
| محافظ `ABSPATH` و ثابت‌های `BEI_*` با پیشوند یکتا | ✅ |
| Settings API (`register_setting` + `settings_fields` + sanitize callback) | ✅ |
| امنیت: nonce (`check_admin_referer`) و بررسی `current_user_can` | ✅ |
| خروجی‌های escape شده (`esc_html`، `esc_attr`، `esc_url`، `esc_textarea`) | ✅ |
| ورودی‌های sanitize شده (`sanitize_text_field`، `sanitize_key`، `wp_unslash`) | ✅ |
| آماده ترجمه (i18n): `__()` ، `esc_html__()` و دامنه `bale-eitaa-notifier` | ✅ |
| بارگذاری استایل/اسکریپت فقط در صفحه خود افزونه (`admin_enqueue_scripts`) | ✅ |
| فایل `uninstall.php` برای پاک‌سازی کامل | ✅ |
| بدون تگ بسته `?>` در پایان فایل‌های PHP | ✅ |

### نصب
1. `wp-bale-eitaa-notifier.zip` را از پیشخوان: **افزونه‌ها ← افزودن ← بارگذاری افزونه** نصب و فعال کنید.
2. به منوی اصلی پیشخوان **«اعلان‌رسان پیام‌رسان‌ها»** بروید، توکن و chat_id هر پیام‌رسان را وارد و ذخیره کنید. (chat_id از چند شناسه پشتیبانی می‌کند — هر شناسه در یک خط)
3. با دکمه‌های «تست بله / تست ایتا» اتصال را بررسی کنید.

### استفاده از هر جای وردپرس (قالب، افزونه‌ها، کد سفارشی)

```php
// ارسال به هر دو پیام‌رسان
bei_notify( 'متن پیام شما' );

// فقط بله / فقط ایتا
bei_notify( 'متن پیام', array( 'bale' ) );
bei_notify( 'متن پیام', array( 'eitaa' ) );

// ارسال با پارامترهای اضافی
bei_eitaa_send( 'پیام سنجاق‌شده', array( 'pin' => 1, 'viewCountForDelete' => 100 ) );
bei_bale_send( 'پیام با دکمه', array(
    'reply_markup' => array(
        'inline_keyboard' => array(
            array( array( 'text' => 'مشاهده', 'url' => 'https://example.com' ) ),
        ),
    ),
) );

// ارسال فایل به ایتا / تصویر به بله
bei_eitaa_send_file( '/path/to/file.pdf', 'توضیح فایل' );
bei_bale_send_photo( 'https://example.com/img.jpg', 'توضیح' );
bei_bale_send_photo( '/path/to/photo.jpg', 'توضیح' );

// تست صحت توکن‌ها
$me_bale  = bei_bale_get_me();
$me_eitaa = bei_eitaa_get_me();
```

---

## ۶) اتصال به سایر افزونه‌ها (فرم‌ها، فروشگاه و...) — مثل ایمیل و پیامک ✅

**بله، کاملاً انجام می‌شود.** افزونه سه روش دارد که دقیقاً همان مدل «اعلان پیامکی» سرویس‌های پیامک را پیاده می‌کند:

### روش ۱ — پل ایمیل سراسری (بدون کدنویسی، برای همه افزونه‌ها) 📧

تقریباً **همه** افزونه‌های وردپرس (فرم‌ها، ووکامرس، عضویت، فراموشی رمز و...) اطلاع‌رسانی را با تابع `wp_mail()` انجام می‌دهند. افزونه ما روی این تابع هوک شده و **هر ایمیلی** که از سایت خارج می‌شود را همزمان به بله/ایتا هم می‌فرستد — دقیقاً مثل اینکه یک سرویس پیامک به ایمیل وصل شده باشد.

- در **تنظیمات ← بله و ایتا ← بخش «پل ایمیل سراسری»** یک تیک بزنید و مقصد (بله/ایتا) را انتخاب کنید.
- با **«فیلتر موضوع»** می‌توانید فقط ایمیل‌های خاص را بفرستید (مثلاً فقط موضوع‌هایی که عبارت «سفارش جدید» دارند).
- پیام به‌صورت خودکار تبدیل می‌شود: موضوع + بدنه (HTML حذف، بخش متنی ایمیل‌های MIME استخراج، کدگذاری‌های base64/quoted-printable باز می‌شود).
- از ارسال تکراری جلوگیری می‌شود و در روند ارسال خود ایمیل هیچ اختلالی ایجاد نمی‌شود.

### روش ۲ — اتصال آماده با یک تیک (برای افزونه‌های معروف) 🔌

در همان صفحه تنظیمات، بخش «اتصال آماده به فرم‌ها و فروشگاه‌ساز»:

| افزونه | گزینه | هوک استفاده‌شده |
|---|---|---|
| Contact Form 7 | ✅ | `wpcf7_mail_sent` |
| WPForms | ✅ | `wpforms_process_complete` |
| Gravity Forms | ✅ | `gform_after_submission` |
| Ninja Forms | ✅ | `ninja_forms_after_submission` |
| Fluent Forms | ✅ | `fluentform_submission_inserted` |
| Elementor Pro Form | ✅ | `elementor_pro/forms/new_record` |
| WooCommerce (وضعیت سفارش‌ها) | ✅ | `woocommerce_order_status_changed` |

هر کدام با یک تیک فعال می‌شود و پیام شامل **جزئیات کامل** (فیلدهای فرم یا اقلام سفارش) است.

### ماژول وضعیت‌های ووکامرس (2.9.0+) 🛒

نحوه کار دقیق:

1. در بخش «اتصال آماده»، ردیف **«ووکامرس»** فقط **نصب بودن ووکامرس** را بررسی می‌کند (چیپ «نصب است / نصب نیست»). سوییچ آن تنها وقتی فعال می‌شود که ووکامرس نصب باشد.
2. با روشن شدن سوییچ، منوی فرعی **«ووکامرس»** زیر منوی اصلی افزونه ظاهر می‌شود — هیچ پیام «سفارش جدید» خودکاری ارسال نمی‌شود (هندلر woocommerce_new_order کاملاً حذف شده است).
3. در منوی فرعی، **وضعیت‌های فعال فروشگاه** به‌صورت خودکار از ووکامرس شناسایی و لیست می‌شوند (شامل وضعیت‌های سفارشی افزونه‌های دیگر).
4. دو تب مجزا: **«📩 پیام‌های مدیر»** و **«👤 پیام‌های مشتریان»** — برای هر وضعیت، هر طرف سوییچ، روش ارسال و متن پیام مخصوص خودش را دارد و کاملاً مستقل ارسال می‌شود. (تنظیمات نسخه‌های قبلی به‌صورت خودکار به این ساختار مهاجرت می‌کند.)
5. متغیرهای مجاز: `{order_id}` ، `{status}` ، `{status_name}` ، `{total}` ، `{currency}` ، `{payment_method}` ، `{customer_name}` ، `{customer_phone}` ، `{customer_email}` ، `{items}` ، `{site_name}` ، `{order_date}` ، `{order_url}`
6. پیام ادمین به شناسه‌های تنظیمات (چند شناسه) ارسال می‌شود؛ پیام مشتری طبق بخش زیر.
7. ⚠️ نکته «در انتظار پرداخت» (pending): این وضعیت، وضعیتِ **اولیه** سفارش است و ووکامرس آن را مستقیم هنگام ساخت سفارش تنظیم می‌کند — هوک تغییر وضعیت برایش اجرا نمی‌شود. افزونه این حالت را جداگانه با هوک `woocommerce_new_order` پوشش می‌دهد؛ پس اگر برای `wc-pending` سوییچ و پیام تنظیم کنید، بلافاصله پس از ثبت هر سفارش جدید اعلانش ارسال می‌شود (با محافظ ضد ارسال تکراری).

### ماژول فرم‌های المنتور (Elementor Pro — 2.10.1) 📝

> نکته مهم: در المنتور پرو فرم‌ها «قالب» جداگانه ندارند — ویجت Form در هر صفحه‌ای ساخته می‌شود. به همین دلیل این ماژول **صفحات منتشرشده** را اسکن می‌کند و ویجت‌های Form داخل آن‌ها را شناسایی می‌کند.

1. در بخش «اتصال آماده»، ردیف **«فرم المنتور (Elementor Pro)»** فقط نصب بودن المنتور پرو را بررسی می‌کند (سوییچ وقتی نصب نباشد غیرفعال است).
2. با روشن شدن سوییچ، منوی فرعی **«فرم‌های المنتور»** ظاهر می‌شود.
3. در این صفحه، **لیست خودکار فرم‌ها** از صفحات مختلف نمایش داده می‌شود — برچسب هر فرم به شکل «عنوان صفحه — نام فرم» است.
4. مدیریت هر فرم فقط دو گزینه دارد: **سوییچ فعال‌سازی + روش ارسال (همه یا یکی از پیام‌رسان‌ها)** — متن پیام به‌صورت خودکار از قالب پیش‌فرض ساخته می‌شود: 📩 فرم «{form_name}» ثبت شد + همه فیلدها.

**تنظیمات داخل خود فرم (Actions After Submit) ⭐:**

علاوه بر این صفحه، یک **اکشن اختصاصی** با نام «اعلان پیام‌رسان‌ها (بله/ایتا/تلگرام/واتساپ)» به فرم‌های المنتور اضافه شده است:

1. در ویرایشگر المنتور، ویجت Form را باز کنید.
2. تب «Actions After Submit» ← «افزودن اکشن» ← **«اعلان پیام‌رسان‌ها»**.
3. تنظیمات همان فرم: سوییچ فعال‌سازی + انتخاب پیام‌رسان (همه یا یکی) + **متن پیام دلخواه** با متغیرها: `{form_name}` ، `{all_fields}` ، `{field:شناسه}` ، `{site_name}` ، `{date}` ، `{page_title}`

هندلر تکراری قدیمی المنتور از اتصال‌های عمومی حذف شده تا پیام دوبار ارسال نشود.

> ⚠️ نکته فنی (خطای 500 رفع‌شده در 2.10.3): کنترل‌های اکشن به «ویجت فرم» اضافه می‌شوند؛ بنابراین هنگام ارسال، مقادیر داخل `$record->get('form_settings')` است — نه داخل خود اکشن. اگر خطای 500 در admin-ajax.php دیدید، نسخه افزونه را به 2.10.3 به‌روزرسانی کنید (روش قدیمی get_settings در المنتور پرو جدید وجود ندارد). محافظ try/catch هم اضافه شده که خطای داخلی اکشن هرگز ارسال فرم را نشکند و فقط در debug.log ثبت شود.

### روش ۳ — کدنویسی دستی با `bei_notify()` (برای هر افزونه دیگر)

کافیست داخل هوک افزونه موردنظر فقط یک خط بنویسید:

```php
bei_notify( 'متن پیام' );
```

هوک‌های آماده برای افزونه‌های پرکاربرد دیگر:

| افزونه | هوک |
|---|---|
| ثبت‌نام کاربر جدید | `user_register` |
| نظر جدید | `comment_post` |
| Formidable Forms | `frm_after_create_entry` |
| Easy Digital Downloads | `edd_complete_purchase` |
| Jetpack Form | `grunion_pre_message_sent` |
| bbPress (موضوع جدید) | `bbp_new_topic` |
| WooCommerce (پرداخت موفق) | `woocommerce_payment_complete` |
| LearnDash (ثبت‌نام دوره) | `learndash_course_completed` |

```php
// ثبت‌نام کاربر جدید
add_action( 'user_register', function ( $user_id ) {
    $user = get_userdata( $user_id );
    bei_notify( "👤 کاربر جدید ثبت‌نام کرد: {$user->display_name}" );
} );

// نظر جدید
add_action( 'comment_post', function ( $comment_id ) {
    $comment = get_comment( $comment_id );
    bei_notify( "💬 نظر جدید از {$comment->comment_author}:\n{$comment->comment_content}" );
} );

// Formidable Forms
add_action( 'frm_after_create_entry', function ( $entry_id, $form_id ) {
    bei_notify( "📝 ورودی جدید در فرم #{$form_id} (شناسه: {$entry_id})" );
}, 10, 2 );

// EDD — پرداخت کامل
add_action( 'edd_complete_purchase', function ( $payment_id ) {
    bei_notify( "💰 پرداخت کامل در EDD: #{$payment_id}" );
} );
```

### 📱 ارسال پیام وضعیت سفارش به «مشتری» — کدام روش ۱۰۰٪ پاسخگو است؟

**حقیقت مهم:** در تلگرام/بله/ایتا ربات نمی‌تواند «خودش» پیام اول را به کسی بدهد — هیچ روشی برای تبدیل شماره موبایل به شناسه گفتگو وجود ندارد (این پلتفرم‌ها شماره را در اختیار ربات نمی‌گذارند). پس هر راه‌حلی که ادعای «ارسال تضمینی به شماره مشتری» بکند، در این سه پیام‌رسان دروغ است.

| روش | پاسخگویی | توضیح |
|---|---|---|
| واتساپ: شماره موبایل مشتری | ⚠️ ناقص | شماره = شناسه است (phone@c.us) ولی شماره +98 پشتیبانی نمی‌شود و مسیر رایگان (CallMeBot) فقط به شماره خودتان می‌فرستد |
| تلگرام/بله/ایتا: پیام سرد به شماره | ❌ غیرممکن | ربات باید قبلاً توسط کاربر Start شده باشد |
| **لینک فعال‌سازی اعلان (پیاده‌سازی‌شده در افزونه)** | ✅ ۱۰۰٪ | مشتری یک‌بار دکمه را می‌زند و ربات را Start می‌کند → شناسه گفتگویش روی سفارش ذخیره می‌شود → از آن پس **هر** پیام وضعیت تضمینی می‌رسد |

**نحوه استفاده از راه ۱۰۰٪ پاسخگو (در افزونه):**

1. در کارت بله/تلگرام تنظیمات، «نام کاربری ربات» را وارد کنید.
2. در صفحه «تشکر از خرید» ووکامرس، شورت‌کد `[bei_subscribe]` را قرار دهید (یا در قالب از هوک `woocommerce_thankyou` استفاده کنید).
3. مشتری روی دکمه «🔔 فعال‌سازی اعلان تلگرام/بله» می‌زند → ربات باز می‌شود → Start می‌زند → افزونه شناسه گفتگوی او را (با توکن امنیتی مخصوص همان سفارش) روی سفارش و پروفایلش ذخیره می‌کند و ربات تأیید می‌فرستد.
4. از این پس، هر تغییری در وضعیت سفارش (پرداخت، ارسال، تکمیل و...) پیام را **تضمینی** به مشتری می‌رساند.

واتساپ هم خودکار تلاش می‌شود: شماره مشتری نرمال می‌شود (۰۹۱۲... → 98912...) و اگر درگاه Green/Ultra فعال باشد به `989...@c.us` می‌فرستد — ولی به‌خاطر محدودیت +98، روش تضمینی همان «لینک فعال‌سازی» تلگرام/بله است.

#### دکمه ایجکس «دریافت شناسه» در صفحه تسویه حساب (رفع نمایش — 2.10.0)

دیگر لازم نیست شورت‌کد را دستی جایی بگذارید. دلایل رایج نمایش‌نیافتن دکمه و رفع آن‌ها:

1. **قالب تسویه حساب بلوکی:** هوک‌های کلاسیک در تسویه حساب بلوکی اجرا نمی‌شوند — از 2.10.0 دکمه‌ها با `wp_footer` به‌صورت نوار شناور پایین صفحه نمایش داده می‌شوند و روی هر دو قالب (کلاسیک و بلوکی) کار می‌کنند.
2. **نام کاربری ربات خالی:** در کارت بله/تلگرام تنظیمات «نام کاربری ربات» را وارد کنید (در صفحه ووکامرس هم هشدار نمایش داده می‌شود).
3. **هیچ وضعیتی در تب «پیام‌های مشتریان» فعال نیست:** دکمه فقط وقتی ظاهر می‌شود که حداقل یک وضعیت با گیرنده مشتری فعال باشد — در تب «👤 پیام‌های مشتریان» حداقل یک وضعیت را روشن کنید.
4. **چک‌لیست تشخیص خودکار:** از 2.10.1 در صفحه ووکامرس (کارت کناری) فهرست «وضعیت نمایش دکمه» با علامت ✅/❌ نمایش داده می‌شود: نصب ووکامرس، سوییچ کلی، نام ربات تلگرام، نام ربات بله و فعال بودن حداقل یک وضعیت مشتری — هر موردی که ❌ است همان علت پنهان‌ماندن دکمه است.
5. از 2.10.1 دکمه از **دو مسیر** نمایش داده می‌شود (هوک کلاسیک `woocommerce_after_checkout_form` و `wp_footer` برای تسویه حساب بلوکی) و باگ مرکزچین CSS هم برطرف شد.

گردش کار دکمه (ایجکس استاندارد admin-ajax):

1. مشتری روی دکمه کلیک می‌کند → ایجکس `bei_subscribe` یک **توکن یکبارمصرف** (۱۵ دقیقه) می‌سازد و ربات را با لینک استارت (`t.me/bot?start=توکن`) باز می‌کند.
2. همزمان سرور در `getUpdates` ربات دنبال همان توکن می‌گردد.
3. مشتری در ربات Start را می‌زند → شناسه گفتگوی او پیدا می‌شود و **در حساب کاربری** (متای `bei_chat_telegram` / `bei_chat_bale`) و سشن فروشگاه ذخیره می‌شود؛ برای مهمان‌ها هم با همان کوکی سشن کار می‌کند و در صفحه تشکر، شناسه سشن به سفارش کپی می‌شود.
4. اگر «ربات پاسخگو» (وبهوک) فعال باشد، مسیر وبهوک جایگزین می‌شود و ایجکس `bei_sub_status` هر ۳ ثانیه وضعیت را بررسی می‌کند تا پیام «✅ شناسه شما دریافت و ذخیره شد» نمایش داده شود.

> از این پس در «ووکامرس ← پروفایل مشتری» (متای کاربر) شناسه‌های ثبت‌شده قابل مشاهده است و هر وضعیت فعال با گیرنده «مشتری»، پیام را تضمینی به همان شناسه می‌فرستد.

### روش ۴ — API خارجی (REST) برای سیستم‌های بیرونی

هر سیستم دیگری (اسکریپت شخصی، ربات، اپلیکیشن و...) می‌تواند پیام بفرستد:

```bash
curl -X POST "https://your-site.com/wp-json/bei/v1/notify" \
  -u "USERNAME:APP_PASSWORD" \
  -H "Content-Type: application/json" \
  -d '{"text": "سلام از سیستم بیرونی", "targets": ["bale", "eitaa"]}'
```

> `APP_PASSWORD` از «کاربران ← پروفایل ← Application Passwords» ساخته می‌شود.

---

### روش ۵ — هوک استاندارد `bei_send` (پیشنهادی برای توسعه‌دهندگان سایر افزونه‌ها ⭐)

برای اینکه «سایر افزونه‌ها» بدون وابستگی کدی از این افزونه پیام بفرستند، یک هوک عمومی استاندارد وردپرس تعریف شده است:

```php
// داخل هر افزونه دیگر — بدون گارد و بدون function_exists:
do_action( 'bei_send', '📢 پیام شما', array( 'bale', 'eitaa' ) );

// فقط یک کانال:
do_action( 'bei_send', 'سفارش جدید رسید', array( 'telegram' ) );
```

- اگر افزونه ما **فعال نباشد**، این فراخوانی بی‌اثر و بی‌خطر است (هیچ خطایی نمی‌دهد) — الگوی استاندارد اتصال افزونه‌ها (مثل هوک‌های ووکامرس)
- کانال‌های مجاز: `bale` ، `eitaa` ، `telegram` ، `whatsapp` — کانال نامعتبر یا خالی → پیش‌فرض (بله + ایتا)

**فیلتر شخصی‌سازی متن هر کانال:**

```php
add_filter(
    'bei_notify_text',
    function ( $text, $target ) {
        if ( 'whatsapp' === $target ) {
            $text = wp_strip_all_tags( $text ); // حذف Markdown برای واتساپ
        }
        return $text;
    },
    10,
    2
);
```

**📚 کتابخانه کامل نمونه‌ها:** فایل `examples/integration-examples.php` داخل افزونه — شامل ۳ روش اتصال + نمونه‌های آماده برای ۲۰+ افزونه معروف (ووکامرس: تغییر وضعیت/کمبود موجودی، ثبت‌نام و ورود کاربر، نظر جدید، بازنشانی رمز، خطای ایمیل، EDD، LearnDash، WP Job Manager، المنتور و...) + نمونه خلاصه روزانه با Cron و جدول مرجع سریع هوک‌ها.

**جدول مرجع سریع هوک‌های پرکاربرد:**

| افزونه / رویداد | هوک |
|---|---|
| ووکامرس: تغییر وضعیت | `woocommerce_order_status_<وضعیت>` |
| ووکامرس: کمبود موجودی | `woocommerce_low_stock` |
| ثبت‌نام / ورود کاربر | `user_register` / `wp_login` |
| نظر جدید | `comment_post` |
| بازنشانی رمز | `password_reset` |
| خطای ایمیل سایت | `wp_mail_failed` |
| EDD: پرداخت کامل | `edd_complete_purchase` |
| LearnDash: پایان دوره | `learndash_course_completed` |
| WP Job Manager: آگهی جدید | `job_manager_job_submitted` |
| bbPress: موضوع جدید | `bbp_new_topic` |

---

## ۷) نکات و محدودیت‌های مهم

### بله
- ربات باید در کانال/گروه **ادمین** باشد تا بتواند پیام بفرستد.
- محدودیت نرخ به‌صورت **تعاملی** محاسبه می‌شود (بر اساس تعامل کاربران با ربات)؛ برای اطلاع‌رسانی انبوه از API کسب‌وکاری (پولی) استفاده کنید.
- آپدیت‌ها تا ۲۴ ساعت (۲۰۰۰ پیام آخر) نگهداری می‌شوند؛ وب‌هوک فقط روی پورت‌های 443 و 88.
- پیام‌ها به‌صورت خودکار Markdown رندر می‌شوند — اگر متن شما ستاره/آندرلاین دارد، اِسکیپ کنید.

### ایتا
- ربات باید **ادمین** کانال/گروه باشد.
- برای گروه از **لینک دعوت** به‌عنوان chat_id استفاده کنید.
- ارسال زمان‌بندی‌شده فقط با پارامتر `date` (Unix Timestamp) ممکن است.
- نام فایل برای گیف `.gif` و برای استیکر `.webp` باشد.
- API «برنامه» (جدید) اعتبار روزانه دارد و کاربر باید برنامه را Start کرده باشد.

### پل ایمیل
- اگر پل ایمیل فعال باشد، **همه** ایمیل‌های سایت (شامل ایمیل‌های مدیریتی) فوروارد می‌شوند — برای محدودسازی از «فیلتر موضوع» استفاده کنید.
- برخی فروشگاه‌ها برای هر سفارش چند ایمیل می‌فرستند (به مدیر و مشتری) — همه فوروارد می‌شوند (طبیعی است).

### مشترک
- هر دو پیام‌رسان از `https` و UTF-8 استفاده می‌کنند (فارسی بدون مشکل).
- همیشه مقدار `ok` در پاسخ JSON را چک کنید.
- توکن‌ها محرمانه‌اند — در کدهای عمومی (جاوااسکریپت سمت کاربر) قرار ندهید، همیشه ارسال را از سمت سرور (PHP) انجام دهید.

---

## ۸) فایل‌های همراه

| فایل | توضیح |
|---|---|
| `wp-bale-eitaa-notifier/wp-bale-eitaa-notifier.php` | فایل اصلی افزونه — بوت‌استرپ و توابع میان‌بُر |
| `wp-bale-eitaa-notifier/includes/*.php` | ۱۰ کلاس ماژولار (Plugin ، Settings ، Messenger ، EmailBridge ، Integrations ، Rest ، IdFinder ، Proxy ، WooStatuses ، ElementorForms) |
| `wp-bale-eitaa-notifier/assets/css/admin.css` | استایل مدرن صفحه تنظیمات (کارت، گرید، سوییچ) |
| `wp-bale-eitaa-notifier/assets/js/admin.js` | اسکریپت دکمه کپی |
| `wp-bale-eitaa-notifier/assets/js/checkout-subscribe.js` + `checkout.css` | دکمه ایجکس «دریافت شناسه» در تسویه حساب |
| `wp-bale-eitaa-notifier/examples/integration-examples.php` | کتابخانه نمونه‌های اتصال برای سایر افزونه‌ها (۳ روش + ۲۰+ هوک آماده) |
| `wp-bale-eitaa-notifier/readme.txt` | معرفی استاندارد افزونه |
| `wp-bale-eitaa-notifier/uninstall.php` | پاک‌سازی داده‌ها هنگام حذف |
| `wp-bale-eitaa-notifier.zip` | نسخه zip آماده نصب از پیشخوان |
| `telegram-relay-worker.js` | اسکریپت رله تلگرام برای Cloudflare Workers (دور زدن فیلترینگ) |
| `callmebot-relay-worker.js` | اسکریپت رله CallMeBot برای Cloudflare Workers (رفع خطای cURL error 28) |
| `test-relay.php` | تست سریع رله تلگرام از خط فرمان (بعد از تست حذفش کنید) |
| `test-send.php` | اسکریپت تست سریع توکن‌ها (مستقل از وردپرس — بعد از تست حذفش کنید) |

### تاریخچه نسخه‌ها
- **3.0.1** — رفع باگ Retry (تلاش مجدد فقط برای کانال‌های ناموفق؛ خطای پیکربندی دائمی است و Retry نمی‌شود)؛ سوییچ «فعال‌سازی» برای هر ۴ پیام‌رسان (غیرفعال = مخفی از همه فهرست‌ها و خروج از ارسال)؛ زیرمنوی «سایر افزونه‌ها» با فهرست مصرف‌کنندگان REST (source/تعداد/زمان/IP + پاک‌سازی) و پارامتر source در نمونه curl.
- **3.0.0** — اجرای اولویت‌های P0/P1 گزارش فنی: (SEC-01) توکن سفارش تصادفی/یکبارمصرف/۷ روزه؛ (ARCH-01) صف ناهمزمان Action Scheduler ← WP-Cron ← مستقیم با سوییچ در پنل؛ (ARCH-02) Retry تا ۳ تلاش با Backoff ۱۰/۶۰ ثانیه؛ (SEC-02) Rate Limit ایجکس ۱۰ در دقیقه؛ (SEC-03) هدر رسمی X-Telegram-Bot-Api-Secret-Token؛ (DEV-01) Logger + کارت «گزارش ارسال»؛ ثابت‌های wp-config برای توکن‌ها؛ مسیردهی همه هوک‌های تولید به صف.
- **2.10.7** — پشتیبانی رسمی چندسایتی در رله تلگرام: دربان‌های اختیاری ALLOWED_BOTS (لیست سفید ربات‌ها) و RELAY_KEY (کلید مشترک) در فایل Worker + فیلد «کلید امنیتی رله» در افزونه (خودکار روی همه درخواست‌های تلگرام).
- **2.10.6** — دکمه «ترمیم خودکار وبهوک»: ثبت مجدد setWebhook با آدرس صحیح (دارای for=) و تأیید با getWebhookInfo — رفع قطعی «Start تلگرام از مسیر بله پاسخ می‌گیرد» وقتی وبهوک با نسخه قدیمی ثبت شده باشد.
- **2.10.5** — دکمه «تست شبیه‌سازی Start» برای ربات پاسخگو: اجرای مستقیم هندلر با آپدیت جعلی (نمایش کانال پاسخ‌دهنده و متن/دکمه) + POST واقعی به آدرس وبهوک (تشخیص 200/401/403/404 با راهنمای علت هر کدام)؛ ثبت تشخیصی آخرین پاسخ وبهوک.
- **2.10.4** — دکمه «بررسی وضعیت وبهوک» (getWebhookInfo): نمایش آدرس ثبت‌شده، آخرین خطای تحویل تلگرام، آپدیت‌های در انتظار و تشخیص آدرس ثبت‌شده با نسخه قدیمی (بدون for=telegram)؛ پیام موفقیت فعال‌سازی با آدرس وبهوک + هشدار تلگرام + هشدار تنظیم‌نبودن رله؛ اعلان‌های چندخطی در پنل.
- **2.10.3** — رفع خطای 500 هنگام ارسال فرم المنتور با اکشن «اعلان پیام‌رسان‌ها»: تنظیمات طبق الگوی رسمی از form_settings رکورد خوانده می‌شود (+ پشتیبان get_settings برای نسخه‌های قدیمی با method_exists) و try/catch مانع شکست ارسال فرم می‌شود.
- **2.10.2** — رفع خطای مرگبار «Call to undefined method add_form_action»: ثبت اکشن المنتور با هوک جدید `elementor_pro/forms/actions_registrar` و متد `register()` + پشتیبانی همزمان از نسخه‌های قدیمی با `method_exists`؛ انتقال بارگذاری ترجمه به `init` (رفع اخطار وردپرس 6.7+).
- **2.10.1** — اسکن فرم‌های المنتور از صفحات منتشرشده (ویجت‌های Form داخل _elementor_data) به‌جای قالب‌ها؛ مدیریت ساده‌شده (فقط سوییچ + روش ارسال)؛ اکشن اختصاصی «اعلان پیام‌رسان‌ها» در Actions After Submit فرم‌های المنتور؛ هوک دوگانه و اصلاح مرکزچین دکمه تسویه حساب؛ چک‌لیست تشخیص ✅/❌ نمایش دکمه؛ بزرگ‌شدن فیلد متن پیام ووکامرس.
- **2.10.0** — تب‌های «پیام‌های مدیر» و «پیام‌های مشتریان» در صفحه ووکامرس (پیام/روش مستقل هر طرف + مهاجرت خودکار تنظیمات قبلی)؛ رفع نمایش دکمه تسویه حساب (wp_enqueue_scripts + wp_footer — سازگار با تسویه حساب بلوکی)؛ ماژول «فرم‌های المنتور» با لیست خودکار فرم‌ها، سوییچ، روش ارسال و متغیرها.
- **2.9.2** — رفع اعلان «در انتظار پرداخت»: وضعیت اولیه سفارش از مسیر woocommerce_new_order اعلان می‌گیرد (با ضد ارسال تکراری)؛ دکمه ایجکس «دریافت شناسه مشتری» در صفحه تسویه حساب (توکن یکبارمصرف + getUpdates/وبهوک + ذخیره در حساب کاربری و سشن).
- **2.9.1** — اصلاح ووکامرس: حذف کامل هندلر woocommerce_new_order از اتصال‌ها (بدون پیام تکراری «سفارش جدید»)؛ سوییچ ووکامرس فقط نصب بودن را بررسی و منوی فرعی را فعال می‌کند؛ غیرفعال‌شدن سوییچ افزونه‌های نصب‌نشده در فهرست اتصال‌ها.
- **2.9.0** — منوی اصلی پیشخوان؛ چند شناسه گفتگو در بله/ایتا/تلگرام/واتساپ (هر شناسه در یک خط)؛ منوی فرعی «ووکامرس» با وضعیت‌های فروشگاه (سوییچ/روش ارسال/گیرنده/متن با متغیرها)؛ ارسال به مشتری با شورت‌کد [bei_subscribe] و deep link (روش ۱۰۰٪ پاسخگو)؛ نام کاربری ربات برای لینک فعال‌سازی.
- **2.8.0** — هوک عمومی `bei_send` برای استفاده سایر افزونه‌ها (بدون وابستگی کدی)؛ فیلتر `bei_notify_text` برای شخصی‌سازی متن هر کانال؛ فایل examples/integration-examples.php با نمونه‌های اتصال برای ۲۰+ افزونه.
- **2.7.0** — عیب‌یابی شبکه: «فورس IPv4» و «مهلت برقراری اتصال» برای درخواست‌های پیام‌رسان‌ها (رفع خطای cURL error 28)؛ فایل رله آماده callmebot-relay-worker.js؛ برچسب «دامنه‌های هدف» در کارت پراکسی.
- **2.6.0** — حذف کامل کانال فیسبوک (اعلان‌های سایت نباید پست عمومی شوند) به‌همراه تمام کدها و فایل رله؛ افزودن مسیر رایگان واتساپ CallMeBot (apikey رایگان، پیام به شماره خودتان)؛ راهنمای کامل «شماره تست» رایگان متا در کارت واتساپ؛ CallMeBot به‌عنوان درگاه پیش‌فرض + هاست آن در کارت پراکسی.
- **2.4.0** — افزودن واتساپ: درگاه‌های میزبانی‌شده Green API و Ultramsg + گزینه رسمی Meta Cloud API؛ «آدرس API سفارشی/رله» برای واتساپ؛ هاست‌های واتساپ در کارت پراکسی؛ دکمه «تست واتساپ».
- **2.3.0** — دکمه «تست پراکسی» در کارت تست اتصال (بررسی تونل پراکسی بدون نیاز به توکن)، مذاکره خودکار روش احراز هویت پراکسی (Basic/Digest/NTLM)، پذیرش URL کامل پراکسی (با کاربر/رمز داخل آدرس) و راهنمای نگاشت مقادیر بخش «تنظیمات پراکسی» Nova-Proxy.
- **2.2.0** — پشتیبانی از تلگرام به‌عنوان پیام‌رسان سوم (sendMessage/sendPhoto/getMe با parse_mode و تلاش مجدد خودکار در خطای قالب‌بندی)، کارت «پراکسی — دور زدن فیلترینگ» (HTTP/SOCKS5 با هوک http_api_curl، فقط روی درخواست‌های API پیام‌رسان‌های انتخاب‌شده)، تنظیم «آدرس API سفارشی/رله» برای تلگرام، گسترش شناسه‌یاب به تلگرام (پروب، getUpdates و ربات پاسخگوی «شناسه شما»).
- **2.1.0** — افزودن ماژول «شناسه‌یاب»: خواندن شناسه عددی با ارسال تست (بله و ایتا)، دریافت گفتگوهای اخیر با getUpdates (بله)، ربات پاسخگوی «شناسه شما» با وبهوک و دکمه اینلاین (بله) و دکمه «استفاده از این شناسه» برای پر کردن خودکار chat_id.
- **2.0.0** — بازنویسی کامل با ساختار استاندارد OOP (WPCS)، پنل تنظیمات مدرن و واکنش‌گرا (کارت‌ها، سوییچ‌های toggle، دکمه کپی، چیپ وضعیت نصب)، افزودن readme.txt و uninstall.php، امنیت کامل (nonce/escaping/sanitize) و پشتیبانی کامل ترجمه.
- **1.1.0** — افزودن پل ایمیل سراسری (wp_mail)، اتصال آماده به CF7/WPForms/Gravity/Ninja/Fluent/Elementor/WooCommerce، API خارجی REST، فیلتر موضوع ایمیل و دکمه تست پل ایمیل.
- **1.0.0** — ارسال پایه به بله و ایتا (sendMessage/sendPhoto/sendFile)، صفحه تنظیمات، دکمه تست، اطلاع‌رسانی انتشار نوشته.

## ۹) منابع رسمی

- مستندات بازوی بله: https://docs.bale.ai
- پنل کسب‌وکار بله: https://business.bale.ai
- راهنمای API ایتایار: https://eitaayar.ir/api/ + [PDF رسمی](https://eitaayar.ir/assets/download/API_eitaayar.ir.pdf)
- درگاه توسعه‌دهندگان ایتا (برنامه و برنامک): https://developer.eitaa.com
- صفحه تست API ایتا: https://eitaayar.ir/testApi
