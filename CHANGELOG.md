# CHANGELOG

## [3.2.1] — 2026-09-14

- **رفع علت «0 bytes / timeout» واتساپ بعد از حل فیلترینگ — محدودیت سرویس رایگان CallMeBot** («Too many requests» با HTTP 503):
  - گلوگاه خودکار برای CallMeBot: فاصلهٔ حداقل ۱۵ ثانیه بین دو ارسال + سقف ۱۵ پیام در ساعت (ترنزینتهای `bei_cmb_last_send` / `bei_cmb_hour_count`) — وقتی رعایت نشود، بدون تماس شبکه خطای `bei_throttled` برمیگردد و صف خودش با تأخیر ۱۰/۶۰ ثانیهای دوباره تلاش میکند
  - تشخیص پاسخهای 503/429 و «Too many requests» کالبوت → خطای **دائمی** `bei_rate_limit` با راهنمای فارسی (تست مکرر نکنید؛ برای ارسال انبوه Green API / Ultramsg) — صف دیگر برای آن Retry نمیفرستد تا سرویس بدتر محدود نشود
- **رفع باگ `multi_send`**: کد خطای داخلی (مثل `bei_rate_limit`) دیگر زیر کد عمومی `bei_multi` گم نمیشود — صف میتواند «دائمی» را از «قابل تکرار» درست تشخیص دهد
- توضیح محدودیت رایگان CallMeBot در کارت واتساپ تنظیمات
- ۵ تست خودکار جدید (مجموع ۱۴ در این نشست) + رگرسیون کامل — همه پاس

## [ابزارها] — 2026-09-14

- **ورکر چندمنظورهٔ جدید `api-relay-worker.js`** (نسخهٔ «سالم» برای فیلد «آدرس API»): یک ورکر برای تلگرام (`/bot...`) + واتساپ CallMeBot (`/whatsapp.php`) + متا (`/{phone_id}/messages`) با:
  - endpoint سلامتی `GET /` و `/health` — تست از مرورگر، سرور و دکمهٔ «بررسی اتصال به رله» افزونه
  - مهلت سختگیرانهٔ بالادست (پیش‌فرض ۱۲ ثانیه) → به‌جای آویزانماندن کلاینت (cURL 28 با ۰ بایت / Empty reply) پاسخ سریع 504 و فعالشدن Failover افزونه
  - بافر کامل بدنه (بدون استریم آویزان) + `cache-control: no-store` + هدر دیباگ `x-bei-relay`
  - دربانها: `RELAY_KEY` (فقط مسیر تلگرام) و `ALLOWED_BOTS` — هر مسیر دیگر 404 (پراکسی باز نمیشود)
  - ۲۶ تست رفتاری خودکار در Node (شبیهساز بالادست) — همه پاس
- endpoint سلامتی `/health` و `no-store` به دو ورکر قبلی (telegram-relay-worker.js و callmebot-relay-worker.js) هم اضافه شد

## [3.2.0] — 2026-09-12

- **علت «اعلان وضعیت سفارش نمیرسد ولی تست میرسد» پیدا و رفع شد** — مسیر پیامهای مشتری کاملاً بیصدا بود:
  - `send_to_customer` هیچ لاگی نداشت و نتیجهٔ ارسال را هم نادیده میگرفت (حتی با خطای API «موفق» برمیگشت)
  - حالا هر فراخوانی در «گزارش ارسال» با رویداد `customer` ثبت میشود: `sent` / `failed` / `skipped` بههمراه دلیل (`no_chat_id` = مشتری Start نزده، `no_phone`، `callmebot_customer` = CallMeBot به مشتری نمیتواند بفرستد) و متن خطای واقعی
- **رفع باگ هدایت اشتباه در صف**: `notify_async` وقتی هیچ کانال فعالی نبود، بیصدا به بله/ایتا میفرستاد (پیام «ارسال میشد ولی به ربات نمیرسید») — حالا رد شدن با لاگ `skipped/no_enabled_targets` ثبت میشود
- **لاگ «queued»**: هر پیامی که وارد صف میشود (Action Scheduler یا WP-Cron) فوراً در گزارش ارسال ثبت میشود — مشخص است پیام وارد مسیر شده یا نه؛ و اگر AS ناموفق بود خودکار به WP-Cron میافتد
- **دکمهٔ «📨 ارسال پیام تست از مسیر واقعی وضعیت سفارش (صف)»** در صفحه ووکامرس: دقیقاً همان مسیر تغییر وضعیت را طی میکند — اگر این پیام برسد ولی اعلان وضعیتها نرسد، مشکل قطعاً در ردیفهای تنظیمات است نه شبکه
- هشدار داخل تب «پیامهای مشتریان»: شرایط ارسال به مشتری (Start/شناسه چت، محدودیت CallMeBot)

## [3.1.9] — 2026-09-12

- **ابزار «بررسی اتصال به رله از سرور» حالا مرحله‌به‌مرحله عیب‌یابی می‌کند**: DNS ← اتصال TCP ← TLS ← پاسخ HTTP با زمان هر مرحله — دقیقاً مشخص می‌شود هر مقصد در کدام مرحله می‌میرد:
  - «اتصال TCP برقرار شد ولی TLS/پاسخ هرگز نرسید» = فیلترینگ SNI (دامنه‌های `*.workers.dev` در ایران همین‌طور فیلترند) + راهنمای اختصاصی ساخت Custom Domain روی ورکر
  - «اتصال برقرار نشد» = فیلترینگ IP یا فایروال هاست
  - تشخیص جداگانهٔ «مسدودکنندهٔ درخواست‌های خارجی وردپرس» (مثل GG External Blocker): اگر مسیر خام شبکه سالم باشد ولی wp_remote خطای فارسی مسدودیت بدهد، هشدار اختصاصی نمایش داده می‌شود
- اسکریپت مستقل `bei-network-diagnose.php` در ریشهٔ ریپو: اجرای مستقیم روی سرور (`php bei-network-diagnose.php`) بدون نیاز به وردپرس — همان کاوش مرحله‌به‌مرحله با خروجی کامل
- افزایش مهلت اجرای تست رله در پنل (تا ۲۴۰ ثانیه — کاوش چند مقصد زمان‌بر است)

## [3.1.8] — 2026-09-12

- **تشخیصپذیری خطاهای شبکه**: نام میزبان (رله یا مسیر مستقیم؟) حالا داخل متن هر خطای cURL میآید — مثل «cURL error 28 … [میزبان: api.telegram.org]» که بلافاصله نشان میدهد درخواست از کدام مسیر رفته است
- **رفع خطای «Cannot parse supplied IRI»** در واتساپ: دنبالکردن Redirect در همه درخواستهای پیامرسانها غیرفعال شد (`redirection => 0`) — این خطا از پردازش هدر Location خرابِ پاسخهای Redirect بود
- **رفع مثبت کاذب در CallMeBot**: پاسخهای HTML خطا (مثل «APIKey is invalid» با HTTP 203) دیگر «ارسال موفق» حساب نمیشوند و متن خطای apikey به کاربر نمایش داده میشود
- **نرمالسازی آدرس رله**: اگر آدرس رله بدون `https://` یا با فاصله/اسلش اضافه وارد شده باشد، خودکار اصلاح میشود (منشأ خطاهای عجیب HTTP)
- **ارتقای فایلهای رله (Cloudflare Worker)**: مهلت ۱۲ ثانیهای برای پاسخ مقصد + بافرکردن کامل بدنه — اگر api.telegram.org/callmebot از سمت کلودفلر آویزان شود، بهجای معلقماندن کلاینت (خطای «0 bytes» و «Empty reply»)، پاسخ سریع 504 برمیگردد و Failover افزونه بلافاصله فعال میشود
- گارد تکرار راهنما در خطاها (هیچ راهنمایی دوبار اضافه نمیشود)

## [3.1.7] — 2026-09-12

- **رفع خطای «cURL error 28: Operation timed out after 10000 milliseconds»** (تلگرام/واتساپ):
  - ریشهٔ مشکل: مهلت برقراری اتصال پیش‌فرض کتابخانهٔ Requests وردپرس ۱۰ ثانیه است و افزونه/میوپلاگین‌های دیگر می‌توانند مهلت کلی را هم به ۱۰ ثانیه محدود کنند — ارسال‌ها قبل از رسیدن پاسخ قطع می‌شدند
  - ماژول پراکسی حالا مهلت اتصال (۱۵ ثانیه) و مهلت کلی را روی خود هندل cURL بازنویسی می‌کند + فیلتر `http_request_args` با اولویت ۹۹۹۹ که مهلت درخواست‌های پیام‌رسان‌ها را هیچ افزونه‌ای کمتر نمی‌کند
  - تنظیم جدید «مهلت کلی هر درخواست» (`bei_http_timeout` / ثابت `BEI_HTTP_TIMEOUT` — پیش‌فرض ۳۰ ثانیه)
- **Failover خودکار در خطاهای شبکه**: اگر رله پاسخ ندهد، ارسال خودکار از مسیر مستقیم (و برعکس) تکرار می‌شود؛ رلهٔ دوم تلگرام (`tg_api_base_alt` / `BEI_TG_API_BASE_ALT`) هم اضافه شد
- **دکمهٔ «🛰️ بررسی اتصال به رله از سرور»**: دسترسی سرور سایت به API مستقیم و همه رله‌ها را با زمان پاسخ اندازه می‌گیرد (سلامت ورکر از بیرون ≠ دسترسی سرور سایت)
- راهنمای فارسی خطاهای شبکه (cURL 28 / DNS / Connection refused/reset / Empty reply) در همه پیام‌های خطا

## [3.1.6] — 2026-09-07

- پشتیبانی از بستهٔ «نسخهٔ آزاد» (ZIP بدون کیت لایسنس): `Bei_License::boot()` بدون کیت دیگر خطای fatal نمی‌دهد و افزونه بدون قفل اجرا می‌شود؛ لینک «لایسنس» در فهرست افزونه‌ها در نسخهٔ آزاد نمایش داده نمی‌شود
- اسکریپت ساخت بسته‌ها (`build-zips.py`): تولید خودکار ZIP کامل (با کیت) و ZIP آزاد (بدون کیت)

## [3.1.5] — 2026-08-20

- راهنمای فارسی خطاهای شناخته‌شده تلگرام/بله در پیام خطا (رفع مسدودیت، chat not found، not enough rights، unauthorized)

## [3.1.4] — 2026-08-20

- فعال‌سازی خودکار اعلان کاربر بعد از ورود (Login): بررسی خودکار متای کاربر + اعلان شناور یک‌کلیکی + ذخیره خودکار شناسه بعد از Start + دکمه رد

## [3.1.3] — 2026-08-20

- رفع بحرانی: پس از فعال‌سازی لایسنس، منوی افزونه و همه امکانات ظاهر نمی‌شد — نمونه‌سازی `Bei_Plugin::instance()` بعد از تأیید لایسنس اضافه شد (هوک‌های admin_menu/REST/پل ایمیل در سازنده کلاس‌ها ثبت می‌شوند)

## [3.1.2] — 2026-08-20

- رفع خطای «product_not_found»: اسلاگ پیش‌فرض محصول به `wp-bale-eitaa-notifier` تغییر کرد (تطابق با محصول سرور فروش)
- ثابت `BEI_PRODUCT_SLUG` برای تغییر اسلاگ از wp-config.php

## [3.1.1] — 2026-08-20

- اتصال لایسنس به افزونه جداگانه «لایسنس منیجر آمل نوین» — حذف صفحه لایسنس مستقل (bei-notify-license)
- لینک «لایسنس» در فهرست افزونه‌ها → صفحه منیجر مرکزی
- اعلان نصب/فعال‌سازی منیجر در پیشخوان (با دکمه دانلود) وقتی افزونه منیجر نصب نیست

## [3.1.0] — 2026-08-20

- سیستم لایسنس و بروزرسانی خودکار (WPLM Client Kit آمل نوین): قفل لایسنس، فرم فعال‌سازی در «تنظیمات ← لایسنس افزونه»، نمایش بروزرسانی در افزونه‌های وردپرس
- اجرای هسته افزونه فقط با لایسنس فعال + ثابت BEI_LICENSE_BYPASS برای مالک
- توابع عمومی در حالت قفل خطای WP_Error می‌دهند (بدون Fatal)

## [3.0.1] — 2026-08-20

- رفع باگ Retry: تلاش مجدد فقط برای کانال‌های «ناموفق» — پیام‌های موفق دیگر تکراری ارسال نمی‌شوند؛ خطای پیکربندی (توکن/شناسه تنظیم نشده) دائمی است و Retry نمی‌شود
- سوییچ «فعال‌سازی» برای هر ۴ پیام‌رسان در تنظیمات — پیام‌رسان غیرفعال از همه فهرست‌ها (روش ارسال، پل ایمیل، پراکسی، دکمه‌های تست، REST) مخفی و از ارسال خارج می‌شود
- زیرمنوی «سایر افزونه‌ها»: فهرست مصرف‌کنندگان API خارجی (REST) با نام source، تعداد ارسال، آخرین زمان و IP

## [3.0.0] — 2026-08-20

اجرای اولویت‌های P0/P1 گزارش فنی:

### امنیت (SEC)
- **SEC-01:** توکن فعال‌سازی اعلان سفارش بازطراحی شد — تصادفی ۳۲ کاراکتری (`wp_generate_password`)، ذخیره در متای سفارش، انقضای ۷ روزه، **یکبارمصرف** (پس از استفاده باطل می‌شود)
- **SEC-02:** Rate Limit ایجکس «دریافت شناسه» — حداکثر ۱۰ درخواست در دقیقه (بر اساس IP)
- **SEC-03:** Secret وبهوک تلگرام به هدر رسمی `X-Telegram-Bot-Api-Secret-Token` منتقل شد (ثبت با پارامتر `secret_token` در setWebhook)؛ کوئری به‌عنوان پشتیبان باقی است
- اولویت خواندن اعتبارنامه‌ها از ثابت‌های `wp-config.php` (`BEI_TG_TOKEN` و...) به‌جای Plain Text در دیتابیس

### معماری (ARCH)
- **ARCH-01:** صف ارسال ناهمزمان — Action Scheduler (پس‌زمینه واقعی) ← WP-Cron تک‌زمانه ← ارسال مستقیم؛ همه هوک‌های تولید (فرم‌ها، ووکامرس، پل ایمیل، المنتور، `bei_send`) به صف مسیردهی شدند
- **ARCH-02:** Retry واقعی — تا ۳ تلاش با Exponential Backoff (۱۰ و ۶۰ ثانیه)

### توسعه‌دهنده (DEV)
- **DEV-01:** ماژول Logger + کارت «گزارش ارسال پیام‌ها» در پنل (۲۰ ارسال آخر: کانال/وضعیت/تلاش/خطا) + دکمه پاک‌سازی
- **QA-02:** GitHub Actions (PHP lint + Node check + ساخت zip) + فایل‌های LICENSE ، SECURITY.md ، CHANGELOG.md

## [2.10.7] — 2026-08-20
- رله چندسایتی تلگرام: دربان‌های `ALLOWED_BOTS` و `RELAY_KEY` + فیلد «کلید امنیتی رله» در افزونه

## [2.10.6]
- دکمه «ترمیم خودکار وبهوک» — ثبت مجدد آدرس با `for=` (رفع پاسخ‌گویی اشتباه از مسیر بله)

## [2.10.5]
- دکمه «تست شبیه‌سازی Start» برای عیب‌یابی ربات پاسخگو (منطق + مسیر HTTP)

## [2.10.4]
- دکمه «بررسی وضعیت وبهوک» (getWebhookInfo: آدرس/خطای آخر تحویل/آپدیت‌های در انتظار)

## [2.10.3]
- رفع خطای 500 ارسال فرم المنتور (خواندن تنظیمات اکشن از form_settings)

## [2.10.2]
- رفع خطای مرگبار ثبت اکشن المنتور (سازگار با Elementor Pro 3.5+) + اصلاح بارگذاری ترجمه

## [2.10.1]
- اسکن فرم‌های المنتور از صفحات + اکشن Actions After Submit + چک‌لیست تشخیص دکمه تسویه حساب

## [2.10.0]
- تب‌های «پیام‌های مدیر / مشتریان» در ووکامرس + ماژول فرم‌های المنتور

## [2.9.x]
- منوی اصلی پیشخوان، چند شناسه گفتگو، ماژول وضعیت‌های ووکامرس، دکمه ایجکس دریافت شناسه مشتری، رفع اعلان وضعیت pending

## [2.8.x و قبل‌تر]
- هوک عمومی `bei_send`، فیسبوک (حذف‌شده)، واتساپ (CallMeBot/متا)، پراکسی و عیب‌یابی شبکه، رله تلگرام، شناسه‌یاب، بازنویسی استاندارد OOP
